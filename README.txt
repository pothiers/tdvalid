Telescope Data VALIDation

Use fits_xml.py (astropy to read FITS header, write XML) to extract
XML exemplars

Use trang (https://github.com/relaxng/jing-trang) to 
generate XML Schema (xsd) from many XML exemplars.
